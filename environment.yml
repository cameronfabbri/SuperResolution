name: superres
channels:
   - pytorch
   - anaconda
   - conda-forge
   - defaults
dependencies:
   - torchvision==0.8.1
   - pytorch==1.7.0
   - tqdm
   - opencv
   - av
   - pip
   - pip:
      - tensorboard
      - pytorch-ssim
